# Known issues

Things in the auth repo that are wrong or misleading today, recorded so an agent is not confused by
them and does not copy them. Fix one only when the task is about it (or with a separate PR); remove
the entry here once it is fixed upstream.

| Issue                                                                                                                                                                                                    | Where                                                                         | Why it matters                                                                                                                                                                                                             |
| -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| The CSRF token is logged at INFO (`Fetched CSRF token: ...`), and the full profile is logged at INFO (`Complete profile information retrieved ...`, `Returning auth result ...`, field-filtered profile) | `app/pesu.py`, `app/app.py`                                                   | Personal data and session tokens in production logs. Do not add more; a fix should drop them to DEBUG or remove the values                                                                                                 |
| `requirements.txt` is stale: pins `httpx` (the code uses `httpx2`) and older `fastapi`/`uvicorn`/`pydantic`/`selectolax` than `pyproject.toml`                                                           | `requirements.txt`                                                            | Nothing builds from it (the Dockerfile uses `uv sync --frozen`), so it misleads rather than breaks. Regenerate with `uv pip compile pyproject.toml -o requirements.txt` when touching dependencies, or propose removing it |
| The bug report template lists `HTML rendering (app/util.py)`, which does not exist                                                                                                                       | `.github/ISSUE_TEMPLATE/bug_report.yaml`                                      | Ignore that option when triaging                                                                                                                                                                                           |
| `tests/unit/test_readme.py` patches the `readme` route and asserts on the mock                                                                                                                           | `tests/unit/test_readme.py`                                                   | Tests nothing; real coverage of `/readme` comes from `test_integration_readme_redirect`                                                                                                                                    |
| Several tests put assertions after the raising call inside `with pytest.raises(...)`                                                                                                                     | `tests/unit/test_pesu.py`, `tests/functional/test_authenticate_functional.py` | Those assertions never run. Do not copy the pattern                                                                                                                                                                        |
| The PR template asks for the title `#IssueNumber - [Description]`, but merged PRs use Conventional Commit titles                                                                                         | `.github/PULL_REQUEST_TEMPLATE.md`                                            | Follow the merged history: Conventional Commit title; issue link in the body                                                                                                                                               |
| The README's request table says `username` is "The user's SRN or PRN"; email and phone also work                                                                                                         | `README.md`                                                                   | The model docs and examples are right; fix the README when editing that section                                                                                                                                            |
| Integration and functional tests that are not marked `secret_required` still call pesuacademy.com                                                                                                        | `tests/integration/`, `tests/functional/`                                     | The "no credentials" run still needs internet access; only `tests/unit` is offline                                                                                                                                         |
| One `DeprecationWarning` from Starlette's test client about `anyio.abc.BlockingPortal`                                                                                                                   | third party                                                                   | Harmless; not ours to fix                                                                                                                                                                                                  |
